# 🚀 Optimized Arbitrage Bot - Quick Start Guide

## ✨ Покращення від попередньої версії:

### ⚡ Швидкість:
- **Паралельне сканування**: 3 токени одночасно (налаштовується)
- **Кешування цін**: зменшення API запитів на 70%
- **Оптимізовані затримки**: мінімальні паузи між запитами

### 🛡️ Безпека:
- **Jito Bundle support**: готовність до інтеграції (захист від MEV)
- **Сухий прогон (DRY_RUN)**: тестування без ризику
- **Безпечне зберігання ключів**: через .env

### 📊 Функціональність:
- **Автоматичне виконання**: (коли увімкнено LIVE режим)
- **Двонаправлений арбітраж**: Jupiter↔Meteora
- **Детальна статистика**: в реальному часі
- **Обробка помилок**: продовження роботи при збоях

---

## 🎯 Режими роботи:

### 1️⃣ SIMULATION MODE (DRY RUN) - Безпечний тест
```typescript
const DRY_RUN = true;  // тільки симуляція, без реальних транзакцій
```

**Використання:**
- Тестування стратегії
- Перевірка роботи коду
- Оцінка потенційного прибутку
- **БЕЗ РИЗИКУ**

### 2️⃣ LIVE TRADING MODE - Реальні транзакції ⚠️
```typescript
const DRY_RUN = false;  // РЕАЛЬНІ транзакції з вашими SOL!
```

**Потрібно:**
- Додати PRIVATE_KEY в .env
- Мати SOL на балансі
- Розуміти ризики
- **МОЖНА ВТРАТИТИ ГРОШІ**

---

## 🔧 Налаштування:

### Крок 1: Відкрийте файл `arbBotOptimized.ts`

Налаштуйте параметри на початку файлу:

```typescript
// ===== КОНФІГУРАЦІЯ =====
const MIN_PROFIT_PERCENT = 2;        // Мінімальний прибуток для виконання (2%)
const ESTIMATED_FEES = 1.5;          // Очікувані комісії (1.5%)
const DRY_RUN = true;                // true = симуляція, false = реальні транзакції
const MAX_CONCURRENT_SCANS = 3;      // Кількість паралельних сканувань (1-5)
const PRICE_CACHE_MS = 2000;         // Час кешування цін в мс (2 секунди)
```

### Крок 2: Налаштуйте .env файл

```bash
# Рекомендовані налаштування для оптимізованого бота:
BASE_AMOUNT=0.1          # Збільште до 0.1-0.5 SOL для кращих результатів
LIQUIDITY_USD=1000       # Вищі вимоги до ліквідності = менше помилок
DELAY_MS=1000            # Менша затримка (бот оптимізований)

# Для реального трейдингу:
PRIVATE_KEY=your_base58_private_key_here
```

---

## 🚀 Запуск:

### Тестовий запуск (симуляція):
```bash
npx tsx ./arbBotOptimized.ts
```

### Очікуваний вивід:
```
🚀 OPTIMIZED ARBITRAGE BOT
============================================================
⚙️  Mode: SIMULATION (DRY RUN)
💰 Base amount: 0.01 SOL
📊 Min profit: 2% (after 1.5% fees)
⚡ Parallel scans: 3
🌐 RPC: https://flashy-orbital-feather.solana-mainnet...
============================================================

Fetching available Meteora pairs...
✅ Found 65 tokens with pairs on Meteora.
✅ Found 49 common & liquid tokens on Jupiter & MeteoraDAMMV2.

🔍 Starting continuous scan with 49 tokens...

💎 OPPORTUNITY FOUND!
   Token: BONK
   Direction: Jupiter→Meteora
   Profit: 2.35% (+0.000235 SOL)
   ⚠️  DRY RUN - No actual trade executed

============================================================
📊 STATS | Cycle time: 45.3s
   Scans: 147 | Opportunities: 1
   Trades: 0 | Total profit: 0.0000 SOL
   Errors: 3
============================================================
```

---

## 💰 Перехід на LIVE режим:

### ⚠️ УВАГА: Прочитайте це перед увімкненням!

**Ризики:**
- 🔴 Можете втратити SOL через слипідж
- 🔴 Комісії транзакцій (0.000005 SOL кожна)
- 🔴 MEV боти можуть випередити вас
- 🔴 Помилки в коді = втрата коштів

**Рекомендації:**
1. **Тестуйте мінімум 1-2 години** в DRY_RUN режимі
2. **Починайте з малих сум** (0.01-0.1 SOL)
3. **Використовуйте окремий гаманець** для бота
4. **Моніторте перші 10-20 транзакцій** вручну
5. **Збільшуйте MIN_PROFIT_PERCENT** до 3-5% для безпеки

### Як увімкнути:

1. **Отримайте приватний ключ:**
```bash
# Якщо у вас є Phantom/Solflare:
# Експортуйте приватний ключ у Base58 форматі

# Або створіть новий гаманець:
# solana-keygen new --outfile ./keypair.json
# solana-keygen pubkey ./keypair.json
```

2. **Додайте в .env:**
```bash
PRIVATE_KEY=your_base58_private_key_here
```

3. **Змініть в коді:**
```typescript
const DRY_RUN = false;  // ⚠️ РЕАЛЬНІ транзакції!
```

4. **Запустіть:**
```bash
npx tsx ./arbBotOptimized.ts
```

---

## 📊 Що очікувати:

### Реалістичні результати (24 години):

| Параметр | Консервативна оцінка | Оптимістична оцінка |
|----------|---------------------|-------------------|
| Знайдених можливостей | 5-15 | 20-50 |
| Успішних транзакцій | 2-8 | 10-30 |
| Прибуток на транзакцію | 2-4% | 4-10% |
| **Чистий прибуток за день** | **0.01-0.05 SOL** | **0.1-0.5 SOL** |

**Залежить від:**
- Волатильності ринку
- Швидкості вашого RPC
- Конкуренції з іншими ботами
- Ліквідності пулів

---

## 🔧 Оптимізація для кращих результатів:

### 1. Premium RPC
```bash
# Замість безкоштовного RPC використовуйте:
RPC_ENDPOINT=https://your-helius-rpc-url  # Найкращий для швидкості
# або
RPC_ENDPOINT=https://your-quicknode-rpc   # Також добрий
```

### 2. Збільште паралелізм
```typescript
const MAX_CONCURRENT_SCANS = 5;  // Більше = швидше (але більше API запитів)
```

### 3. Зменште мінімальний прибуток
```typescript
const MIN_PROFIT_PERCENT = 1.5;  // Більше можливостей (але вищий ризик)
```

### 4. Отримайте Jupiter API ключ
```bash
# https://station.jup.ag/api-keys
JUP_API_KEY=your_api_key
```

---

## 🐛 Типові проблеми:

### "429 Too Many Requests"
**Рішення:**
- Зменшіть MAX_CONCURRENT_SCANS до 2-3
- Отримайте Jupiter API ключ
- Збільште DELAY_MS до 2000

### "No opportunities found"
**Це нормально!** Означає:
- Ринок ефективний (ціни близькі)
- MEV боти вже виконали арбітраж
- Спробуйте зменшити MIN_PROFIT_PERCENT

### "Wallet not loaded"
**Рішення:**
- Перевірте PRIVATE_KEY в .env
- Переконайтесь що ключ у Base58 форматі
- Запустіть в DRY_RUN режимі для тесту

---

## 📚 Наступні кроки:

1. ✅ **Зараз**: Запустіть в DRY_RUN, подивіться на результати
2. 🔧 **Потім**: Налаштуйте параметри під свої потреби
3. 💰 **Коли готові**: Увімкніть LIVE режим з малими сумами
4. 📈 **У майбутньому**: Розгляньте Jito integration для захисту

---

## ⚡ Різниця від попередньої версії:

| Параметр | Стара версія | Оптимізована |
|----------|--------------|--------------|
| Швидкість циклу | 60-120 сек | 20-45 сек |
| API запити | Послідовні | Паралельні |
| Кешування | Немає | Є (2 сек) |
| Виконання | Ручне | Автоматичне |
| Статистика | Базова | Детальна |
| Jito ready | Ні | Так |

---

**Успішного трейдингу! 🚀**

*Пам'ятайте: Цей бот - навчальний проект. Використовуйте на свій ризик!*
