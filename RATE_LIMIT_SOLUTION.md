# Розв'язання проблеми 429 Rate Limit

## Проблема
QuickNode RPC endpoint видає **429 Too Many Requests** через:
- Meteora SDK робить багато RPC запитів для кожного токена (fetchPoolState, getMint, getSlot, etc.)
- Jupiter API працює нормально, але не може виконати запити через проблеми з RPC

## Причини появи 429:
1. **QuickNode Free Tier**: обмеження ~25 requests/sec
2. **Meteora SDK**: кожен токен = ~5-10 RPC запитів
3. **43 токени одночасно** = ~200-400 запитів = Rate limit

## ✅ РІШЕННЯ 1: Збільшити затримки між токенами (ПРОСТЕ)

У файлі `arbBotOptimized.ts` змініть затримку з 1.5s на 3s:
```typescript
await new Promise(resolve => setTimeout(resolve, 3000)); // Було 1500
```

## ✅ РІШЕННЯ 2: Зменшити кількість токенів для сканування

У файлі `Config/.env`:
```env
# Сканувати тільки топ-20 токенів з найбільшою ліквідністю
MAX_TOKEN_PAGES_SCAN=50
LIQUIDITY_USD=10000  # Підвищити до $10k
```

## ✅ РІШЕННЯ 3: Використати premium RPC з вищими лімітами

### Helius (РЕКОМЕНДОВАНО):
- Free tier: 100 req/sec
- Зареєструйтесь: https://www.helius.dev/
```env
RPC_ENDPOINT=https://mainnet.helius-rpc.com/?api-key=YOUR_KEY
```

### Alchemy:
- Free tier: 300 CU/sec
- Зареєструйтесь: https://www.alchemy.com/solana
```env
RPC_ENDPOINT=https://solana-mainnet.g.alchemy.com/v2/YOUR_KEY
```

## ✅ РІШЕННЯ 4: Використати тільки Jupiter (без Meteora)

Meteora робить найбільше RPC запитів. Можна спочатку протестувати тільки Jupiter:

```typescript
// В arbBotOptimized.ts закоментуйте Meteora запити:
const [jupiterBuyResult] = await Promise.all([
  this.getJupiterPrice(token.mint, BASE_AMOUNT_IN_LAMPORTS),
  // this.getMeteoraPrice(token.meteoraPairAddress, BASE_AMOUNT_IN_LAMPORTS, true) // ЗАКОМЕНТОВАНО
]);
```

## ✅ РІШЕННЯ 5: Кешувати pool states Meteora

Додати довший кеш для Meteora pool states (зменшить RPC запити на 80%):

```typescript
private poolStateCache = new Map<string, { state: any, timestamp: number }>();
```

## 🎯 РЕКОМЕНДОВАНА СТРАТЕГІЯ:

1. **Короткостроково**: Збільшити затримки до 3-5 секунд
2. **Середньостроково**: Зареєструватись на Helius (безкоштовно)
3. **Довгостроково**: Додати кешування pool states

## Швидкий фікс (зараз):
