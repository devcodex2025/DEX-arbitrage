# 🔐 Налаштування гаманця для Live Trading

## ⚠️ ПОПЕРЕДЖЕННЯ

**ПЕРЕД ПОЧАТКОМ ПРОЧИТАЙТЕ:**

1. ❌ **НЕ використовуйте ваш основний гаманець** з великими сумами
2. ✅ **Створіть окремий гаманець** тільки для бота
3. 💰 **Тримайте мінімальні суми** (0.1-0.5 SOL для тестів)
4. 🔒 **Ніколи не комітьте .env файл** у git
5. ⚡ **Почніть з малих сум** та DRY_RUN режиму

---

## 🚀 Швидкий старт

### Крок 1: Запустіть утиліту налаштування

```bash
npx tsx ./utils/walletSetup.ts
```

### Крок 2: Оберіть опцію

**Варіант A: Створити новий гаманець** ✅ Рекомендовано
- Виберіть опцію `1`
- Скопіюйте згенерований приватний ключ (Base58)
- Збережіть публічну адресу для поповнення

**Варіант B: Імпортувати існуючий**
- Якщо маєте Phantom/Solflare: експортуйте приватний ключ
- Якщо маєте файл `~/.config/solana/id.json`: виберіть опцію `2`

### Крок 3: Додайте ключ у .env

Відкрийте `Config/.env` та додайте:

```bash
PRIVATE_KEY=your_base58_private_key_here
```

### Крок 4: Поповніть гаманець

Переведіть SOL на публічну адресу з попереднього кроку:
- Мінімум: 0.05 SOL (для комісій + тестів)
- Рекомендовано: 0.1-0.5 SOL

---

## 📋 Покрокова інструкція

### 1️⃣ Створення нового гаманця (рекомендовано)

```bash
# Запустіть утиліту
npx tsx ./utils/walletSetup.ts

# Оберіть: 1 (Створити новий гаманець)
# Побачите:
```

```
✅ Гаманець створено!
============================================================
Публічний адрес: 7xKXtg2CW87d97TXJSDpbD5jBkheTqA83TZRuJosgAsU
Приватний ключ (Base58): 5Jv...ABC (довгий рядок)
============================================================
```

**Що робити далі:**
- ✅ Скопіюйте приватний ключ
- ✅ Додайте в `Config/.env`: `PRIVATE_KEY=5Jv...ABC`
- ✅ Скопіюйте публічний адрес
- ✅ Поповніть його SOL через Phantom/Coinbase/Binance

---

### 2️⃣ Імпорт з Phantom/Solflare

**Phantom:**
1. Відкрийте Phantom
2. Settings → Security & Privacy → Export Private Key
3. Скопіюйте ключ
4. Запустіть `npx tsx ./utils/walletSetup.ts`
5. Оберіть опцію `3` (конвертувати у Base58)
6. Вставте ключ

**Solflare:**
1. Settings → Export Wallet → Show Private Key
2. Скопіюйте Base58 ключ
3. Додайте напряму у `.env`

---

### 3️⃣ Імпорт з Solana CLI

Якщо ви використовуєте Solana CLI:

```bash
# Знайдіть ваш keypair файл (зазвичай)
# ~/.config/solana/id.json

# Запустіть утиліту
npx tsx ./utils/walletSetup.ts

# Оберіть: 2 (Імпортувати з JSON)
# Введіть шлях: ~/.config/solana/id.json
# або C:\Users\YourName\.config\solana\id.json (Windows)
```

---

## 🔒 Безпека

### Перевірка .gitignore

Переконайтеся що `.env` є у `.gitignore`:

```bash
# Перевірте
cat .gitignore | grep .env

# Якщо немає - додайте
echo "Config/.env" >> .gitignore
echo "*.env" >> .gitignore
echo "wallet-backup.json" >> .gitignore
```

### Права доступу до файлів

**Linux/Mac:**
```bash
chmod 600 Config/.env
```

**Windows:**
- Клік правою → Properties → Security
- Залиште доступ тільки для вашого користувача

---

## ✅ Перевірка налаштування

### Тест 1: Перевірка балансу

```bash
# Створіть тестовий скрипт
npx tsx -e "
import { Connection, PublicKey, LAMPORTS_PER_SOL } from '@solana/web3.js';
const conn = new Connection('https://api.mainnet-beta.solana.com');
const wallet = new PublicKey('YOUR_PUBLIC_KEY');
const balance = await conn.getBalance(wallet);
console.log('Balance:', balance / LAMPORTS_PER_SOL, 'SOL');
"
```

### Тест 2: Запуск бота у DRY_RUN

```bash
# Бот завантажить гаманець але не виконає транзакції
npx tsx ./arbBotOptimized.ts

# Побачите:
# ✅ Wallet loaded: 7xKXtg2CW87d97TXJSDpbD5jBkheTqA83TZRuJosgAsU
```

---

## 💰 Поповнення гаманця

### Через Phantom

1. Відкрийте Phantom
2. Натисніть "Receive"
3. Відскануйте QR або скопіюйте адресу
4. **АБО** введіть адресу вашого бот-гаманця
5. Переведіть 0.1-0.5 SOL

### Через CEX (Binance/Coinbase)

1. Зайдіть у CEX
2. Знайдіть SOL
3. Withdraw → Solana Network
4. Вставте адресу бот-гаманця
5. Сума: 0.1-0.5 SOL
6. Підтвердьте

⚠️ **Перевірте адресу двічі!** Невірна адреса = втрата коштів

---

## 🎯 Фінальні кроки перед запуском

### 1. Оновіть Config/.env

```bash
# Повний приклад .env файлу
JUP_API_KEY=your_api_key_here

BASE_TOKEN_MINT=So11111111111111111111111111111111111111112
BASE_TOKEN_SYMBOL=SOL
BASE_TOKEN_DECIMALS=9
BASE_AMOUNT=0.1

SLIPPAGE_BPS=50

RPC_ENDPOINT=https://your-rpc-endpoint

MAX_TOKEN_PAGES_SCAN=500
LIQUIDITY_USD=1000
DELAY_MS=2000

# ⚠️ ДОДАЙТЕ ЦЕ:
PRIVATE_KEY=your_base58_private_key_here
```

### 2. Налаштуйте параметри бота

Відкрийте `arbBotOptimized.ts` та налаштуйте:

```typescript
// Для першого запуску
const MIN_PROFIT_PERCENT = 5;     // Високий поріг = безпечніше
const DRY_RUN = true;             // ТРИМАЙТЕ true для тестів!
const MAX_CONCURRENT_SCANS = 2;    // Менше = стабільніше
```

### 3. Перший тестовий запуск (симуляція)

```bash
# Запустіть у DRY_RUN режимі
npx tsx ./arbBotOptimized.ts

# Чекайте 5-10 хвилин
# Переконайтеся що:
# - Гаманець завантажується ✅
# - Знаходяться можливості ✅
# - Немає помилок ✅
```

### 4. Перехід на LIVE режим

**⚠️ ТІЛЬКИ ПІСЛЯ УСПІШНИХ ТЕСТІВ!**

```typescript
// arbBotOptimized.ts
const DRY_RUN = false;  // ⚠️ РЕАЛЬНІ ТРАНЗАКЦІЇ!
```

**Перший реальний запуск:**
- Запустіть на 5-10 хвилин
- Моніторте КОЖНУ транзакцію
- Перевіряйте баланс після кожної операції
- Якщо щось не так - ЗУПИНІТЬ (Ctrl+C)

---

## 📊 Моніторинг

### Перевірка транзакцій

```bash
# Ваші транзакції будуть тут:
# https://solscan.io/account/YOUR_PUBLIC_KEY
```

### Перевірка балансу

```bash
# У реальному часі
npx tsx -e "
import { Connection, PublicKey, Keypair } from '@solana/web3.js';
import bs58 from 'bs58';
import dotenv from 'dotenv';
dotenv.config({ path: './Config/.env' });

const conn = new Connection(process.env.RPC_ENDPOINT!);
const keypair = Keypair.fromSecretKey(bs58.decode(process.env.PRIVATE_KEY!));
const balance = await conn.getBalance(keypair.publicKey);
console.log('Balance:', balance / 1e9, 'SOL');
"
```

---

## 🆘 Типові проблеми

### "Wallet not loaded"

**Причина:** Приватний ключ не знайдено або невірний формат

**Рішення:**
```bash
# Перевірте .env
cat Config/.env | grep PRIVATE_KEY

# Має бути:
PRIVATE_KEY=5Jv8...ABC (довгий Base58 рядок, ~88 символів)
```

### "Insufficient funds"

**Причина:** Недостатньо SOL на балансі

**Рішення:**
- Перевірте баланс на solscan.io
- Поповніть гаманець
- Мінімум 0.05 SOL для комісій

### "Transaction failed"

**Причини:**
- Slippage занадто високий
- Ліквідність пулу змінилась
- MEV бот випередив вас

**Рішення:**
- Збільште `MIN_PROFIT_PERCENT`
- Зменшіть `BASE_AMOUNT`
- Отримайте кращий RPC

---

## ✅ Чеклист готовності

Перед запуском у LIVE режимі переконайтеся:

- [ ] Створив окремий гаманець для бота
- [ ] Додав PRIVATE_KEY у .env
- [ ] .env файл у .gitignore
- [ ] Поповнив гаманець (0.1+ SOL)
- [ ] Отримав Jupiter API ключ
- [ ] Налаштував premium RPC
- [ ] Протестував у DRY_RUN режимі (1+ годину)
- [ ] Встановив високий MIN_PROFIT_PERCENT (5%+)
- [ ] Готовий моніторити транзакції
- [ ] Розумію ризик втрати коштів

---

**Готові почати? Запустіть:**

```bash
npx tsx ./utils/walletSetup.ts
```

🎯 **Успіху у трейдингу!**
